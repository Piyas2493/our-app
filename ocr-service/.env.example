# Origin the Next.js frontend runs at, for CORS. Change if you run
# `npm run dev` on a non-default port.
OCR_FRONTEND_ORIGIN=http://localhost:3000

# Override only if you want to try a different TrOCR checkpoint.
# TROCR_MODEL_NAME=microsoft/trocr-base-handwritten
